
# Transient atlas download cache, EXCEPT the two NIfTIs the static site needs
# served directly (GitHub Pages has no server.py to fetch them on demand).
cache/*
!cache/aal.nii.gz
!cache/mni152.nii.gz

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*~

# Local overrides
*.local

# Claude Code local settings
.claude/
